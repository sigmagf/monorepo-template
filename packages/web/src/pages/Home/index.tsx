import React from 'react';

import { HomePageContainer } from './styles';

export const HomePage: React.FC = () => {
  return (
    <HomePageContainer>
      TEMPLATE REPOSITORY
    </HomePageContainer>
  );
};
